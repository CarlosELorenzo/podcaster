export type PodcastType = {
  id: string;
  title: string;
  author: string;
  description: string;
  imageUrl: string;
};

export type EpisodeType = {
  id: string;
  title: string;
  date: string;
  duration: string;
  description: string;
  episodeUrl: string;
};
